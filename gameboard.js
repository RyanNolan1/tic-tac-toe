const theGameStructure = (function () {
  const gameBoard = {
    rows: 3,
    columns: 3,
    gameArray: ["", "", "", "", "", "", "", "", "ga"],
  };

  return gameBoard;

  //   const playerFactory = (name) => {
  //     return name;
  //   };

  //   const gameFlow = {};
})();

export default theGameStructure;
